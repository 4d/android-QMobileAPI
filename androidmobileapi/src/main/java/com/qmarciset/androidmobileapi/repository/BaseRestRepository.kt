package com.qmarciset.androidmobileapi.repository

import okhttp3.ResponseBody
import org.json.JSONObject
import retrofit2.Response

abstract class BaseRestRepository {

//    abstract fun authenticate(
//        jsonRequest: JSONObject,
//        onResult: (isSuccess: Boolean, response: Response<ResponseBody>?, error: Any?) -> Unit
//    )

//    fun login(onResult: (isSuccess: Boolean, response: Response<ResponseBody>?, error: Any?) -> Unit)

//    abstract fun getAllFromApi(onResult: (isSuccess: Boolean, response: ResponseBody?, error: Any?) -> Unit)
}
